
Model Training

python3 train.py <checkpoint_file> <colored_image> <monochrome_image> <num_epochs> <device> <learning_rate>

all the parameters have assigned default values.

Project execution

python3 main.py <checkpoint_file> <test_image> <output_path>

all the parameters have assigned default values.

The final output is generated by the name "final_generated_image.jpg", if default parameters used.